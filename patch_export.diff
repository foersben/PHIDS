--- tests/unit/pipeline/test_export.py
+++ tests/unit/pipeline/test_export.py
@@ -10,12 +10,15 @@
 def test_export_json() -> None:
     with tempfile.NamedTemporaryFile(suffix=".duckdb", delete=False) as f:
         tmp_path = f.name
-    pathlib.Path(tmp_path).unlink()
+    try:
+        pathlib.Path(tmp_path).unlink()
+    except Exception:
+        pass

     # Create dummy DataFrames
     flora = pl.DataFrame(
