1. **Verify Refactor Structure:** Ensure the `movement` logic has been split into `math.py` and `core.py` within `src/phids/engine/systems/interaction/movement/`. All fallback and fast path definitions correctly maintain typing, tests continue to pass and performance regression via complex imports is absent.
2. **Commit pre-commit steps:** Complete pre-commit steps to ensure proper testing, verification, review, and reflection are done.
3. **Submit Code:** Request a code review and push changes to git with git submit.
