# .github/workflows/secrets.env.example
# Copy this file to 'secrets.env' in the same directory and fill in your real token if needed.
# DO NOT commit the actual 'secrets.env' file to source control.

GITHUB_TOKEN=your_real_github_token_here
