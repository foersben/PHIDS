name: CI & Deployment Pipeline

on:
  push:
    branches: [ "main", "develop" ]
  pull_request:
    branches: [ "main", "develop" ]

permissions:
  contents: read
  pages: write      # Required for GitHub Pages
  id-token: write   # Required for GitHub Pages

env:
  UV_FROZEN: "true"
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: "true"

jobs:
  quality-gate:
    name: Code & Context Quality
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4

      - name: Install uv & Python 3.13
        uses: astral-sh/setup-uv@v8.3.2
        with:
          enable-cache: true
          python-version: '3.13'

      - name: Install dependencies
        run: uv sync --all-groups

      - name: Run Pre-Commit Hooks
        run: SKIP=check-identity,enforce-author-identity uv run pre-commit run --all-files

      - name: Pass 1 - Logic & Coverage
        env:
          NUMBA_DISABLE_JIT: "1"
        run: uv run pytest --cov=src/phids --cov-fail-under=80 -o "addopts="

      - name: Pass 2 - Numba Compilation
        run: uv run pytest tests/integration/systems/test_interaction_property_invariants.py -x -q -o "addopts="
  deploy-docs:
    name: Build & Deploy Documentation
    needs: quality-gate # Only deploys if tests and pre-commit pass
    if: github.ref == 'refs/heads/main' && github.event_name == 'push'
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v6

      - name: Install uv & Python 3.13
        uses: astral-sh/setup-uv@v8.3.2
        with:
          enable-cache: true
          python-version: '3.13'

      - name: Install dependencies
        run: uv sync --group dev

      - name: Build Docs with Zensical
        run: uv run zensical build --strict

      - name: Upload artifact
        if: ${{ !env.ACT }}
        uses: actions/upload-pages-artifact@v5
        with:
          path: ./site

      - name: Deploy to GitHub Pages
        if: ${{ !env.ACT }}
        id: deployment
        uses: actions/deploy-pages@v5
