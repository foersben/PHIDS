{
  "mcpServers": {
    "PHIDS-Orchestrator": {
      "command": "uv",
      "args": [
        "run",
        "python",
        "-m",
        "phids.mcp_server"
      ],
      "env": {
        "PYTHONPATH": "${workspaceFolder}/src"
      },
      "cwd": "${workspaceFolder}"
    }
  }
}
